 * build into a minimal container
 * build leader and follower container
 * use postgresql as the backing store

 * work out an initialization process
   - build ecmnet
   - start postgres (incl creating user accounts and db users)
   - run initialization script
   - start ecmnet server
   - start ecmnet client(s)
